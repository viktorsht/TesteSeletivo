name: go_fidelidade
description: "Teste seletivo para o guia de móteis"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.6.1
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.3.0
  mockito: ^5.4.5
  mocktail: ^1.0.4
  auto_injector: ^2.0.5
  go_router: ^14.7.2
  flutter_svg: ^2.0.17
  skeletonizer: ^1.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/sirene.png
    - assets/fire.svg
